# kitchen-recipe プロジェクト 作業ガイド

## ファイル構成と役割

| ファイル | 役割 | アプリへの影響 |
|---|---|---|
| `menu-data.js` | **メニュー・レシピの本体データ** | **直接反映される** |
| `kana-map.js` | ネパール語表示時のかな読みマッピング | **直接反映される** |
| `app.js` | アプリのロジック | **直接反映される** |
| `index.html` | アプリのHTML | **直接反映される** |
| `data.json` | ⚠️ 旧スプレッドシート出力ファイル（未使用） | **アプリに読み込まれない** |

## 重要：献立・レシピを編集するときは必ず `menu-data.js` を使う

`data.json` はアプリから読み込まれていない。過去に `data.json` を編集してしまい、アプリに反映されないという事例が発生している。

献立やレシピの追加・修正は **`menu-data.js`** に対して行うこと。

## 献立を追加・修正するときのチェックリスト

1. `menu-data.js` の該当メニュー（`seasons.summer.menus` など）を編集する
2. ネパール語訳（`name_ne`, `category_ne`）も必ず同時に追加する
3. `kana-map.js` の `items` セクションに `name_ja` と完全一致するキーでかな読みを登録する
4. `data.json` は触らない

## 注記の書き方

メモや注意書きは `※` で始める（例: `※肉は冷凍しない`）。
`※` 始まりのテキストは `app.js` の `formatMenuAnnotations()` により自動で色付き表示になる。
